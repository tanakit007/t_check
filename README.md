# T-Check (MVP) - AI Grammar Corrector

เว็บแอปพลิเคชันสำหรับตรวจทานไวยากรณ์ภาษาไทยและอังกฤษ, ปรับสำนวน (Humanizer), และเรียบเรียงประโยคใหม่ (Paraphraser) โดยใช้ AI (รองรับทั้ง Local LLM ผ่าน LM Studio และ API บน Cloud)

## ✨ ฟีเจอร์หลัก
*   **Editor แบบเอกสาร**: หน้าตาใช้งานง่ายเหมือน Google Docs / Word
*   **3 โหมดอัจฉริยะ**:
    1.  **Grammar Check**: ตรวจแก้ไวยากรณ์ (ไทย/อังกฤษ)
    2.  **Humanizer**: ปรับสำนวนให้เป็นธรรมชาติ (มีให้เลือก 4 เสียง: Everyday, Official, etc.)
    3.  **Paraphraser**: เรียบเรียงประโยคใหม่ (มีให้เลือก 5 สไตล์: Academic, Creative, etc.)
*   **รองรับภาษาไทย 100%**: เมนูและคำสั่ง AI เปลี่ยนเป็นภาษาไทยอัตโนมัติเมื่อเลือกโหมด TH
*   **Save & Export**: บันทึกเป็นไฟล์ .html หรือจำลองการส่งออกไปยัง Google Docs

## 🛠️ การติดตั้งและรันโปรเจกต์

ต้องมี [Node.js](https://nodejs.org/) ติดตั้งอยู่ในเครื่องก่อน

1.  **ติดตั้ง Dependencies**:
    ```bash
    npm install
    ```

2.  **รันเซิร์ฟเวอร์ (Development Mode)**:
    ```bash
    npm run dev
    ```
    เปิด Browser ไปที่: `http://localhost:5173`

3.  **สร้างไฟล์ Production (Build)**:
    ```bash
    npm run build
    ```

---

## 🤖 การตั้งค่า AI (LM Studio Setup)

แนะนำให้ใช้ร่วมกับ **LM Studio** เพื่อใช้งานฟรีบนเครื่องตัวเอง (ไม่ต้องเสียค่า API)

### 1. ติดตั้งและดาวน์โหลดโมเดล
*   ดาวน์โหลด [LM Studio](https://lmstudio.ai/)
*   ค้นหาและดาวน์โหลดโมเดลที่รองรับภาษาไทย แนะนำ: `scb10x/llama-3-typhoon-v1.5-8b-instruct`

### 2. เปิด Server (Local Inference Server)
*   ไปที่แท็บ **Local Server** (ไอคอนลูกศรสองหัว `<->`) ด้านซ้าย
*   เลือกโมเดลที่โหลดมาตรงด้านบน
*   **การตั้งค่า Configuration** (ขวา):
    *   **Port**: `1234` (ค่าเริ่มต้น)
    *   **CORS**: ✅ **Enable CORS** (สำคัญมาก! ต้องติ๊กถูก)
*   กดปุ่ม **Start Server**

### 3. ตั้งค่าในเว็บ T-Check
1.  ในหน้า ChatPage กดปุ่ม **Settings** (รูปเฟืองมุมซ้ายล่าง)
2.  ตั้งค่าดังนี้:
    *   **API Key**: ใส่อะไรก็ได้ (เช่น `lm-studio`)
    *   **Base URL**: `http://localhost:1234/v1`
    *   **Model Name**: `scb10x_-_llama-3-typhoon-v1.5-8b-instruct` (หรือชื่อโมเดลที่คุณโหลดมา)
3.  กด **Save Configuration**

---

## 📂 โครงสร้างโปรเจกต์

```
src/
├── components/     # UI Components (Header, Modal, etc.)
├── contexts/       # Global State (LanguageContext)
├── pages/          # หน้าหลัก (HomePage, ChatPage, Login)
├── routers/        # การตั้งค่า Route
├── services/       # aiService.js (ตัวเชื่อมต่อ API)
└── App.jsx         # Main Component
```

## ⚠️ ปัญหาที่พบบ่อย (Troubleshooting)

*   **Error: Unexpected endpoint / 404**:
    *   เช็ค Base URL ว่าต้องลงท้ายด้วย `/v1` (ไม่ต้องมี `/chat/completions`)
    *   โปรแกรมมีระบบ Auto-fix ให้แล้ว ลองกด Save ใหม่
*   **Error: Failed to fetch / CORS**:
    *   คุณลืมเปิด **Enable CORS** ใน LM Studio
*   **ข้อความภาษาไทยตอบมาเป็นภาษาอังกฤษ**:
    *   ตรวจสอบว่ากดสลับภาษาบนเว็บเป็น **TH** หรือยัง
    *   โมเดลบางตัวอาจจะไม่เก่งภาษาไทย แนะนำให้ใช้ `Typhoon` หรือ `OpenThaiGPT`

---
*Created by Antigravity*
